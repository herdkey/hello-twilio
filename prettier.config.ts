// prettier.config.ts
import type { Config } from 'prettier';

const config: Config = {
  semi: true,
  singleQuote: true,
  bracketSpacing: true,
  trailingComma: 'all',
  printWidth: 80,
};

export default config;
